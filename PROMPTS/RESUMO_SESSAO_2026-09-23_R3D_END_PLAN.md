# Session summary — 2026-09-23 — R3D reviewed, the end of the plan rewritten, R3D-SPIKE-3D measured and ruled

**Resume point:** `RENDER3D_MASTER_PLAN` **v1.20**. The plan is complete: every stage to the end of the track is written.
Nothing on the road is built yet. **Next session starts R3D-8 (the safety net on the 3D path).** Every commit is pushed.
No `verified/` tag was asked for.

## What happened, in order

1. **The week's R3D work reviewed** (R3D-0 to R3D-7 plus SOOT-STAMP, 182 commits 2026-09-14 → 09-22), with an inventory of
   what the 3D default still takes from the 2D.
2. **The end of the plan rewritten** (`6a58766d`).
   - **The Director's ruling:** *"o importante é a gente não depender mais do sistema anterior pra continuar"*. The 2D
     retires on INDEPENDENCE, not on look; the look is no longer a gate.
   - **Renames:** R3D-8 → **R3D-END**, and the old R3D-9 → **R3D-ROT**.
   - **The road, in order:**
     - R3D-8, the net;
     - R3D-9, the 3D board reads nothing of the 2D;
     - R3D-10, the simulation writes no tiles;
     - R3D-11, gameplay off `floor_layer`;
     - R3D-12, the load builds no 2D board;
     - R3D-13, one path and the last baseline;
     - then R3D-END.
   - Floors and slabs show the whole facade. The rename was carried across every doc and four code comments.
3. **The legacy audit** (the v1.19 block holds the full list with sources). The headline items:
   - the Moto's 3D boot still bakes **~20.3 s** of 2D atlases out of 53.3 s;
   - the cook's PACKAGE still resolves 2D tiles on the 3D board;
   - `Board3DLive._read_look()` reads the soot and tone uniforms from the 2D face shader;
   - the 3D board mirrors sprites that the 2D renderer creates (glass cracks, floor piles);
   - `floor_layer` has 168 references in 38 files;
   - the 63 selftests are pinned to `RENDER3D=0`;
   - rotation and `SaveState` lose 21 voxels of damage (open since 2026-09-16).
4. **Decisions:** the web export is retired and the APK is the phone test (§8 Q4 closed). The 2D reference set, taken once at
   R3D-8, goes to `ARCHIVE/`.
5. **FIX, rotation (`1df84605`).**
   - **Symptom (Director's screenshot):** after a rotation the walls stayed in the old view, while the agent, guards, cones,
     lamps and fog moved to the rotated cells.
   - **Cause:** `_set_perspective()` rebuilt the store and never the 3D board.
   - **Fix:** it now rebuilds `Board3DLive` after the damage, glass and light are re-stamped. `_start_board3d_live()` also
     removes the old board from the tree at once, because a queued node keeping the name would have renamed the new one.
   - **Verified:** red on the old code (one board build per session); after the fix, one build per rotation W → S → N with
     the actors on the floor. 63 selftests clean.
6. **R3D-SPIKE-3D** (`98e3518e` … `a62b3a6c`). Moto g04s, PLAYGROUND, portrait, zoom 0.5, one boot per row.
   - **S1, the board lit by 12 real `OmniLight3D` lamps:**
     - 43.6 ms/frame without shadows, against 19.5 today;
     - 58.4 ms with 4 shadowed lamps;
     - 66–73 ms with all 12 shadowed.
     - **Director: the board keeps its CPU light buckets.**
   - **S2, live actors:**
     - `tools/asset_generation/r3d_live_rig_export.py` keys the 32 real walk phases (`p3_walk_export`) into a `walk`
       action. No `.blend` holds any action.
     - It joins the 62 bake parts into one skinned mesh: 7 608 tris, 11 materials, `agent_live_walk.glb`, which is
       git-ignored and regenerated by the script.
     - Cost of 9 walking rigs: +2.4 ms with actor-layer lamps; **+1.0 ms with `spike_mesh_planes.gdshader`**, which has no
       Godot light and reads the floor cell's bucket from the board's `cell_plane` array.
   - **S3, static props:**
     - 20 single-mesh props: +9.7 ms with lamps; **+1.5 ms with the planes shader**.
     - 200k-tri stand-ins: +4.3 ms. That is an asset budget, not a renderer limit.
   - **Director: S2 and S3 APPROVED** → `ACTOR` **D64** and **D65**, new stages **R3D-ACTORS** and **R3D-PROPS** (they may start
     after R3D-8). R3D-WORLD shrinks to overlays + VFX, R3D-LIGHT becomes the light's CPU cost, and R3D-GLB is absorbed.
7. **Docs at session close:** RENDER3D v1.20 and ACTOR v2.3 (D64, D65). Notes were added to the CHARACTER, MOVEMENT, MATERIALS,
   VOXEL_LIGHT, DEVICE_DIAGNOSTICS, PERFORMANCE and LIGHT plans, `character_bake_pipeline.md`, `MobileTesting.md`, the
   CLAUDE.md reference rows, `docs/README.md` and `current_state.md`.

## Verified this session

- Lint and invariants clean, CODEMAP regenerated, 63 selftests clean (after the rotation fix).
- Every Moto number above comes from `FRAME_PROBE` lines in `docs/measurements/device_2026-09-23_moto_spike3d_*.log`.

## Open, recorded, not done

- **The spike code stays until R3D-ACTORS**, which promotes the shader and the rig export and deletes the rest:
  - `Spike3D`, and the `Board3DLive.LIT3D` board variant that S1 rejected;
  - the flags `LIGHT3D`, `ACTOR_MESH`, `PROP_MESH`, `PROP_KIND`, `PROP_NOLAMPS`, `MESH_PLANES`.
- **Actors on meshes, not done:**
  - the weapon in the hand;
  - soot and the cutaway on a mesh;
  - the `AgentSprite` facing / pose bridge;
  - RAM against the atlases;
  - every other motion keyed as an action;
  - the design question D44 left open: does gameplay keep four facings?
- **Unexplained:** one boot of the rig APK exited during the load, with nothing in the crash or kill logs (not reproduced).
- Rotation was verified on an undamaged PLAYGROUND only (no blast before rotating). The rebuild cost on the Moto was not
  measured.
- **From the audit:** the Galaxy's 3D shot tail (357 ms vs 214 ms on 2D, one boot) must be explained at R3D-13 while the 2D
  still exists.

## Traps met

- **zsh does not word-split `$VAR`:** a stored `adb -s <serial>` became one command name, so the first device batch installed
  and pushed nothing. Use a bash script or a shell function.
- **Mesh cost on the Moto is the lighting model, not the geometry:** any Godot light costs per covered pixel. Measure a mesh
  with and without lamps before blaming the triangles.
